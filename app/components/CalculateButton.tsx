'use client'
import React from 'react'

const CalculateButton = () => {
  return (
    <div>
      <button onClick={() => alert('Hello')}>Calculate</button>
    </div>
  )
}

export default CalculateButton
